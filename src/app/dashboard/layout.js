export const metadata = {
  title: 'Dashboard - ProductZone',
  description: 'User management dashboard for ProductZone',
};

export default function DashboardLayout({ children }) {
  return children;
}
